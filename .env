# ========================
# 🔧 Configuración General
# ========================
ENVIRONMENT=development

# ========================
# 🐘 Config Base de Datos
# ========================
POSTGRES_DB=mydb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# ========================
# ☕ Config Backend
# ========================
SPRING_PORT=8080
# Estas variables con ${} no funcionan dentro del .env en Docker Compose
# Son útiles dentro del código o archivos Java, pero no aquí.
# Se deben declarar por separado.
# Por eso puedes eliminarlas de aquí si solo están para documentación.
# Si las necesitas en tu backend (application.properties), deben ir allí.
# SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/${POSTGRES_DB}
# SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
# SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD}
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# ========================
# ⚛️ Config Frontend
# ========================
FRONTEND_PORT=3000
VITE_API_URL=http://backend:8080

# ========================
# 📧 Config SMTP (Password Recovery)
# ⚠️ No lo estás usando por ahora, así que lo puedes comentar o dejar como está.
# ========================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=tu_email@example.com
SMTP_PASSWORD=tu_password_seguro
SMTP_FROM=noreply@example.com
