# ========================
# 🔧 Configuración General
# ========================
ENVIRONMENT=development

# ========================
# 🐘 Config Base de Datos
# ========================
POSTGRES_DB=mydb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ========================
# ☕ Config Backend
# ========================
SPRING_PORT=8080
SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/mydb
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=postgres
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# ========================
# ⚛️ Config Frontend
# ========================
FRONTEND_PORT=80
# 👇 Muy importante: si el frontend se sirve en http://localhost,
# y quieres que las llamadas API vayan a http://localhost/api (proxy),
# esto debe apuntar al backend **como lo ve el navegador del usuario**:
VITE_API_URL=http://localhost:8080
# Si el frontend se sirve en un dominio diferente, ajusta esto:
# VITE_API_URL=http://tu-dominio.com/api
# Si el proxy de Nginx está configurado para redirigir `/api` hacia el backend, esto es correcto.

# ========================
# 📧 Config SMTP
# ========================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=tu_email@example.com
SMTP_PASSWORD=tu_password_seguro
SMTP_FROM=noreply@example.com
